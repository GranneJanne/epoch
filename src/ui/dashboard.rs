use ratatui::Frame;
use ratatui::layout::Rect;

use crate::app::App;

pub fn render_dashboard(_frame: &mut Frame, _area: Rect, _app: &App) {
    todo!()
}

#[cfg(test)]
mod tests {
    #[test]
    fn test_placeholder() {
        assert!(true);
    }
}
