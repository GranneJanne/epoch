use ratatui::Frame;
use ratatui::layout::Rect;

use crate::config::Config;

pub fn render_header(_frame: &mut Frame, _area: Rect, _config: &Config) {
    todo!()
}

#[cfg(test)]
mod tests {
    #[test]
    fn test_placeholder() {
        assert!(true);
    }
}
